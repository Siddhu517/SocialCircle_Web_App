let Post,User,mongoose;_0dc‍.x([["createPost",()=>createPost],["newsFeed",()=>newsFeed],["likePost",()=>likePost],["unlikePost",()=>unlikePost],["sharePost",()=>sharePost],["totalPosts",()=>totalPosts],["postsByUser",()=>postsByUser],["userPost",()=>userPost],["updatePost",()=>updatePost],["createComment",()=>createComment],["deleteComment",()=>deleteComment],["editComment",()=>editComment],["posts",()=>posts],["getPost",()=>getPost],["getPostedImages",()=>getPostedImages],["getPostedVideos",()=>getPostedVideos],["getPostedDocuments",()=>getPostedDocuments],["getPostsFriends",()=>getPostsFriends]]);_0dc‍.w("../Models/post",[["default",["Post"],function(v){Post=v}]]);_0dc‍.w("../Models/user",[["default",["User"],function(v){User=v}]]);_0dc‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);



       const createPost = async (req, res) => {
  try {
    const { content, resFiles, file_type, bodyText, postBy } = req.body;
    //console.log(content, resFiles, file_type, bodyText, postBy);

    if (!content) {
      return res.json({
        error: "Content is required",
      });
    }

    if (!resFiles && !bodyText) {
      return res.json({
        error: "Images is required",
      });
    }

    if (!file_type) {
      return res.json({
        error: "type is required",
      });
    }

    let post;
    let shareBy;

    if (!postBy) {
      shareBy = "public";
    } else {
      shareBy = postBy;
    }

    if (file_type === "image") {
      post = new Post({
        _id: new mongoose.Types.ObjectId(),
        content,
        file_type,
        images: resFiles,
        postedBy: req.user._id,
        postShareBy: shareBy,
      });
    } else if (file_type === "video") {
      post = new Post({
        _id: new mongoose.Types.ObjectId(),
        content,
        file_type,
        videos: resFiles,
        postedBy: req.user._id,
        postShareBy: shareBy,
      });
    } else if (file_type === "document") {
      post = new Post({
        _id: new mongoose.Types.ObjectId(),
        content,
        file_type,
        documents: resFiles,
        postedBy: req.user._id,
        postShareBy: shareBy,
      });
    } else if (file_type === "text") {
      post = new Post({
        _id: new mongoose.Types.ObjectId(),
        content,
        file_type,
        body_content: bodyText,
        postedBy: req.user._id,
        postShareBy: shareBy,
      });
    } else {
      return res.json({
        error: " file_type is unknown",
      });
    }

    await post.save();

    const user = await User.findByIdAndUpdate(
      req.user._id,
      {
        $addToSet: {
          posts: post._id,
        },
      },
      { new: true }
    );

    const postData = await Post.findById(post._id).populate(
      "postedBy",
      "-password"
    );

    res.json({
      postData,
      status: "ok",
      message: "Successfully Post Created",
    });
    
  } catch (err) {
    _0dc‍.g.console.log(err);
    res.status(400).json({
      error: "Error",
    });
  }
};

       const newsFeed = async (req, res) => {
  try {
    const user = await User.findById(req.params.id);
    let following = user.following;
    following.push(req.user._id);

    //const posts = await Post.find({})
    const posts = await Post.find({ postedBy: { $in: following } })
      .populate("postedBy", "_id name username image")
      .populate("comments.postedBy", "_id name username image")
      .sort({ createdAt: -1 });

    res.json(posts);
  } catch (err) {
    _0dc‍.g.console.log(err);
  }
};

       const likePost = async (req, res) => {
  const { id } = req.body;
  try {
    const post = await Post.findByIdAndUpdate(
      id,
      {
        $addToSet: { likes: req.user._id },
      },
      { new: true }
    );
    res.json({
      status: "ok",
      data: post,
    });
  } catch (err) {
    _0dc‍.g.console.log(err);
    res.json({
      error: err,
    });
  }
};

       const unlikePost = async (req, res) => {
  const { id } = req.body;
  try {
    const post = await Post.findByIdAndUpdate(
      id,
      {
        $pull: { likes: req.user._id },
      },
      { new: true }
    );
    res.json({
      status: "ok",
      data: post,
    });
  } catch (err) {
    _0dc‍.g.console.log(err);
    res.json({
      error: err,
    });
  }
};

       const sharePost = async (req, res) => {
  //console.log(req.body)
  const { id } = req.body;

  try {
    const post = await Post.findByIdAndUpdate(
      id,
      {
        $push: {
          shares: {
            shareBy: "Friends",
            postedBy: req.user._id,
          },
        },
      },
      { new: true }
    )
      .populate("postedBy", "_id name username image")
      .populate("comments.postedBy", "_id name username image");
    _0dc‍.g.console.log(post);

    /* write code to send  friend posts *********** */

    res.json({
      status: "ok",
      message: "successfully send post",
      data: post,
    });
  } catch (err) {
    _0dc‍.g.console.log(err);
    res.json({
      error: err,
    });
  }
};

/* total post count */
       async function totalPosts(req, res) {
  //console.log(req.params._id);
  try {
    const post = await Post.findById(req.params._id); /* ({
      postedBy: req.body.id,
    }); */
    _0dc‍.g.console.log(post);
    res.json(post);
  } catch (err) {
    _0dc‍.g.console.error(err);
    res.status(500).send(err);
  }
}

       const postsByUser = async (req, res) => {
  try {
    const posts = await Post.find({ postedBy: req.user._id })
      //const posts = await Post.find()
      .populate("postedBy", "_id name image")
      .sort({ createdAt: -1 });
    // console.log('posts',posts)
    res.json(posts);
  } catch (err) {
    _0dc‍.g.console.log(err);
  }
};

       const userPost = async (req, res) => {
  try {
    const post = await Post.findById(req.params._id)
      .populate("postedBy", "_id name image")
      .populate("comments.postedBy", "_id name image");
    res.json(post);
  } catch (err) {
    _0dc‍.g.console.log(err);
  }
};

       const updatePost = async (req, res) => {
  // console.log("post update controller => ", req.body);
  try {
    const post = await Post.findByIdAndUpdate(req.params._id, req.body, {
      new: true,
    });
    res.json(post);
  } catch (err) {
    _0dc‍.g.console.log(err);
  }
};

       const createComment = async (req, res) => {
  const { comment, id, postedById } = req.body.data;
  _0dc‍.g.console.log(comment, id, postedById);
  try {
    if (!comment || !id || !postedById) {
      return res.json({
        error: "error",
      });
    }

    const commentByUser = await User.findById({ _id: postedById });
    if (!commentByUser) {
      return res.json({
        error: "no user found",
      });
    }

    const userPost = await Post.findByIdAndUpdate(
      id,
      {
        $push: {
          comments: {
            _id: id + "-" + Date.now(),
            comment: comment,
            postedBy: commentByUser._id,
            username: commentByUser.username,
            image: commentByUser.image,
          },
        },
      },
      { new: true }
    )
      .populate("postedBy", "_id name image")
      .populate("comments.postedBy", "_id name username image");
    _0dc‍.g.console.log(userPost);

    res.json({
      message: "success",
      status: "ok",
      data: userPost,
    });
  } catch (err) {
    _0dc‍.g.console.log(err);
  }
};

       const deleteComment = async (req, res) => {
  _0dc‍.g.console.log(req.body);
  const { id, commentId } = req.body.data;
  _0dc‍.g.console.log(id, commentId);
  if (!id || !commentId) {
    return res.json({
      error: "error",
    });
  }

  try {
    const post = await Post.findByIdAndUpdate(
      id,
      {
        $pull: { comments: { _id: commentId } },
      },
      { new: true }
    );

    res.json({
      message: "remove comment",
      data: post,
    });
  } catch (err) {
    _0dc‍.g.console.log(err);
  }
};

       const editComment = async (req, res) => {
  const { id, newComment, commentId } = req.body.data;
  _0dc‍.g.console.log(id, newComment, commentId);
  try {
    if (!id || !newComment || !commentId) {
      return res.json({
        error: "error",
      });
    }

    /* update */
    const userPost = await Post.findByIdAndUpdate(
      id,
      {
        $set: {
          "comments.$[elem].comment": newComment,
        },
      },
      {
        arrayFilters: [{ "elem._id": commentId }],
        new: true,
      }
    );

    return res.json({
      message: "comment update",
      status: "ok",
      data: userPost,
    });
  } catch (err) {
    _0dc‍.g.console.error(err.message);
    return res.status(500).send("Server Error");
  }
};

       const posts = async (req, res) => {
  try {
    const posts = await Post.find()
      .populate("postedBy", "_id name image")
      .populate("comments.postedBy", "_id name image")
      .sort({ createdAt: -1 });
    res.json(posts);
  } catch (err) {
    _0dc‍.g.console.log(err);
  }
};

       const getPost = async (req, res) => {
  try {
    const post = await Post.findById(req.params._id)
      .populate("postedBy", "_id name image")
      .populate("comments.postedBy", "_id name image");
    res.json(post);
  } catch (err) {
    _0dc‍.g.console.log(err);
  }
};

/* get images */

       const getPostedImages = async (req, res) => {
  try {
    const post = await Post.find({
      postedBy: req.user._id,
      file_type: "image",
    });
    _0dc‍.g.console.log(post);
    res.json(post);
  } catch (err) {
    _0dc‍.g.console.log(err);
  }
};

/* get videos */

       const getPostedVideos = async (req, res) => {
  try {
    const post = await Post.find({
      postedBy: req.user._id,
      file_type: "video",
    });
    _0dc‍.g.console.log(post);
    res.json(post);
  } catch (err) {
    _0dc‍.g.console.log(err);
  }
};

/* get documents */

       const getPostedDocuments = async (req, res) => {
  try {
    const post = await Post.find({
      postedBy: req.user._id,
      file_type: "document",
    });
    _0dc‍.g.console.log(post);
    res.json(post);
  } catch (err) {
    _0dc‍.g.console.log(err);
  }
};

/* **************** */

/* friend all posts get */
       const getPostsFriends = async (req, res) => {
  _0dc‍.g.console.log("posts friends =>  ", req.params.username);
  try {
    const user = await User.findOne({ username: req.params.username });
    /*   let following = user.following;
    following.push(mongoose.Types.ObjectId(req.params.id)); */
    //following.push(req.params.username);

    const posts = await Post.find({ postedBy: user })
      .populate("postedBy", "_id name username image")
      .populate("comments.postedBy", "_id name username image ")
      .sort({ createdAt: -1 });
    /*  const posts = await Post.find({ postedBy: { $in: following } })
      .populate("postedBy", "_id name username image")
      .populate("comments.postedBy", "_id name username image")
      .sort({ createdAt: -1 });
 */
    res.json(posts);
  } catch (err) {
    _0dc‍.g.console.log(err);
  }
};
