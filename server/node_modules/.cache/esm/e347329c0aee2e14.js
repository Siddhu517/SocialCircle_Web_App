let express,mongoose,cors,dotenv,Auth,Post;_0ae‍.w("express",[["default",["express"],function(v){express=v}]]);_0ae‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_0ae‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_0ae‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);_0ae‍.w("./Routes/Auth",[["default",["Auth"],function(v){Auth=v}]]);_0ae‍.w("./Routes/Post",[["default",["Post"],function(v){Post=v}]]);






dotenv.config();

const app = express();
const http = require("http").createServer(app);

//Middleware
app.use(express.json({ limit: "5mb" }));
app.use(express.urlencoded({ extended: true }));
app.use(
  cors({
    origin: [process.env.CLIENT_URL],
  })
);

//db connection
mongoose
  .connect(process.env.DATABASE, {
    useNewUrlParser: true,
    useUnifiedTopology: true,
  })
  .then(() => console.log("DB connected"))
  .catch((err) => _0ae‍.g.console.log("DB Connection ERROR", err));

app.use("/api", Auth);
app.use("/api", Post);

const port = process.env.PORT || 8000;

http.listen(port, () => console.log(`Server running on port :${port}`));
